# Clustering-Pipeline-on-UCI-Pen-Based-Digits
This notebook implements a full clustering pipeline (K‑Means, Hierarchical, DBSCAN) on the Pen‑Based Recognition of Handwritten Digits dataset from UCI. It includes preprocessing, PCA, hyper‑parameter tuning, evaluation with internal/external metrics, and visualization.
